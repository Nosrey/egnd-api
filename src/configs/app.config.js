const appConfig = {
  apiPrefix: '/api',
  authenticatedEntryPath: '/inicio',

  unAuthenticatedEntryPath: '/iniciar-sesion',
  tourPath: '/',
  locale: 'en',
  enableMock: true,
};

export default appConfig;
