const appConfig = {
  apiPrefix: '/api',
  authenticatedEntryPath: '/home',

  unAuthenticatedEntryPath: '/construccion',
  tourPath: '/',
  locale: 'en',
  enableMock: true,
};

export default appConfig;
